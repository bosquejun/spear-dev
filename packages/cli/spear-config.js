// eslint-disable-next-line @typescript-eslint/no-require-imports
const { defineConfig } = require("@spear/sdk");

module.exports = defineConfig({
	gui: {
		theme: {
			defaultTheme: "light",
		},
	},
});
